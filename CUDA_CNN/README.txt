Projekt enhält Source-Code in src. Darin sind Klassen für alle Layer, ein CNN namens network.cpp und die CUDA-Kernels.
Des weiteren sind die MNIST Trainings- und Testdaten in den Ordnern train und test.
Debug und Release enthalten von NSIGHT Eclipse erstellte outputs und binaries.
